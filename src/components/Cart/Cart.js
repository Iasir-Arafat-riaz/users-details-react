import React from 'react';

const Cart = (props) => {
    return (
        <div>
            <h1>count = {props.total.length}</h1>
            <p>ekhane</p>
        </div>
    );
};

export default Cart;